{
  "name": "Ejercicio_Video",
  "version": "0.0.2",
  "description": "Ejercicio video",
  "repository": "https://github.com/kolaso13/Ejercicio_Video.git",
  "author": "kolaso13 <adrbrarare@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "parcel build *.html",
    "prebuild": "(test -d dist || mkdir dist) && cp -rv Video dist",
    "prestart": "yarn prebuild",
    "start": "parcel serve *.html --open"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.1",
    "@parcel/transformer-sass": "2.0.1",
    "autoprefixer": "^10.3.7",
    "parcel": "^2.0.1",
    "postcss-uncss": "^0.17.0",
    "posthtml-cli": "^0.10.0",
    "posthtml-extend": "^0.6.0",
    "posthtml-include": "^1.7.2"
  }
}
